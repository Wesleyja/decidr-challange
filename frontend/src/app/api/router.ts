import peopleEndpoints from './people'
import uploadDataEndpoints from './uploadData'

const endpoints = {
    people: peopleEndpoints,
    uploadData: uploadDataEndpoints
}

export default endpoints
